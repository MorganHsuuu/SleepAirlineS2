# ── Notion（二選一，學生只需一把 key）──────────────────────
#   路 A（簡單）：只填主辦的 NOTION_API_KEY → 連主辦共用主庫，資料進論文總表。
#   路 B（進階）：填「自己的」NOTION_API_KEY ＋「自己的」NOTION_PARENT_PAGE_ID
#                 → 程式自動在你自己的 Notion 建表，資料只進自己的空間。
NOTION_API_KEY=ntn_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
NOTION_PARENT_PAGE_ID=388a7f1b413c8015824ff6fb8bc1d65b

# 資料模式（選填）
# preview — 不寫 Notion，fork 改 UI 用（Vercel 不設 NOTION_API_KEY 時自動 preview）
# live    — 寫入主庫（有 NOTION_API_KEY 時預設 live，主辦部署維持此行為）
# SLEEP_AIRLINE_DATA_MODE=live

# 選填：直接指定主庫 / 風景圖庫 DB ID（程式只讀取，絕不更動其結構）
# fork 接上主辦主庫時請用主辦提供的 ID；留空則只會在父頁面「尋找」既有主庫，
# 找不到會直接報錯（不再自動建表），避免 fork 建出孤島表污染論文數據。
# NOTION_DASHBOARD_DB_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# NOTION_LANDSCAPE_DB_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ⚠️ 僅主辦首次建立主庫時短暫開啟；學生 fork 請勿設定。
# 預設（不設或非 true）：程式只讀取主庫、絕不更動或新建結構。
# NOTION_ALLOW_SCHEMA_WRITE=true

# OpenAI（選填，機長廣播文字 + 語音 + 降落風景生圖）
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_MODEL=gpt-4o-mini
OPENAI_IMAGE_MODEL=gpt-image-1-mini
# OPENAI_TTS_MODEL=tts-1
# OPENAI_TTS_VOICE=onyx
